pipeline {
  agent {
    node {
        label 'golang-agent-docker'
    }
  }

  stages {

    stage('Docker') {
      steps {
        sh 'docker build -t my-image .'
      }
    }

    stage('Run') {
      steps {
        sh 'docker run --rm my-image'
      }
    }
  }
}
